import { Component } from "react";

class Topbar extends Component {
    render() {
        return(
            <div className="topbar">
                Topbar
            </div>
        );
    }
}

export default Topbar;